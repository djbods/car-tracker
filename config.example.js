// Copy this file to config.local.js and fill in the values.
// config.local.js is gitignored — never commit real keys.
// In production, Netlify generates config.local.js at deploy time
// from the SUPABASE_URL and SUPABASE_PUBLISHABLE_KEY environment variables.
window.APP_CONFIG = {
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_PUBLISHABLE_KEY: 'your-publishable-key',
};
